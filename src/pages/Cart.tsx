import React from "react";
import { useAppSelector } from "../hooks/redux";

function Cart() {
    return (
        <div className="Cart">
            <h1>Cart</h1>
            
        </div>
    );
}

export default Cart;
